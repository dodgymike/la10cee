#include "connectionstate.h"

/*
std::ostream & operator<< (std::ostream & os, ConnectionState state) {
	switch (state) {
		case STATE_NEW: os << "NEW"; break;
		case STATE_CONNECTING: os << "CONNECTING"; break;
		case STATE_CONNECTED: os << "CONNECTED"; break;
		case STATE_CLOSING: os << "CLOSING"; break;
		case STATE_RESET: os << "RESET"; break;
		case STATE_CLOSED: os << "CLOSED"; break;
		case STATE_UNKNOWN: os << "UNKNOWN"; break;
	}

	return os;
}
*/

